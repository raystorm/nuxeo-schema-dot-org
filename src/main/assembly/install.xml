<install>
  <copy file="${package.root}/${project.build.finalName}.${project.packaging}"
      tofile="${env.bundles}"/>
  <deploy file="${env.bundles}/${project.build.finalName}.${project.packaging}"/>
</install>